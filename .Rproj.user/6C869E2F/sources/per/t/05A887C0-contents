#  This function will convert a string with numbers into numbers
#' the function output a numeric vector that represents numbers previously represented as strings
#' @description the function convert numbers that are strings to numbers
#' @param x a character vector
#' @examples

numerize = function(x){as.numeric(as.character(x))}
